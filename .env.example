# Optional: cached token (not used for auth flow; script mints a fresh token each run).
# AXIOM_ACCESS_TOKEN=

# Preferred: client credentials used to mint OAuth token.
AXIOM_CLIENT_ID=your_client_id
AXIOM_CLIENT_SECRET=your_client_secret
